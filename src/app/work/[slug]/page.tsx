import Image from "next/image";
import Link from "next/link";
import { notFound } from "next/navigation";
import { projects } from "@/data/projects";
export function generateStaticParams(){return projects.map(({slug})=>({slug}))}
export default async function ProjectPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const project=projects.find(p=>p.slug===slug);if(!project)notFound();return <main style={{padding:"150px 0 110px"}}><div className="container"><div className="eyebrow">{project.category}</div><h1 className="display" style={{marginTop:28,fontSize:"clamp(4rem,10vw,9rem)"}}>{project.title}</h1><p style={{maxWidth:720,fontSize:20,color:"var(--muted)",lineHeight:1.65,marginTop:30}}>{project.description}</p><div style={{display:"flex",flexWrap:"wrap",gap:9,marginTop:28}}>{project.technologies.map(t=><span className="pill" key={t}>{t}</span>)}</div><div style={{position:"relative",aspectRatio:"16/9",borderRadius:32,overflow:"hidden",marginTop:64,border:"1px solid var(--line)"}}><Image src={project.image} alt={project.title} fill priority style={{objectFit:"cover",objectPosition:"top"}}/></div><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:60,marginTop:84}} className="case-grid"><div><div className="eyebrow">The challenge</div><h2 style={{fontSize:38,letterSpacing:"-.04em"}}>Create presence, not decoration.</h2><p style={{color:"var(--muted)",lineHeight:1.8}}>The project needed a clear point of view, premium visual credibility, responsive performance, and a structure capable of growing with the business.</p></div><div><div className="eyebrow">The solution</div><h2 style={{fontSize:38,letterSpacing:"-.04em"}}>Design and technology working as one.</h2><p style={{color:"var(--muted)",lineHeight:1.8}}>A custom experience combining strong art direction, focused content hierarchy, modern frontend architecture, and deliberate interaction design.</p></div></div><Link href="/#work" className="btn" style={{marginTop:64}}>← Back to selected work</Link></div><style>{`@media(max-width:700px){.case-grid{grid-template-columns:1fr!important;gap:30px!important}}`}</style></main>}
